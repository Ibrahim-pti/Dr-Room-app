// Deprecated video reel item - replaced by Medical Articles Hub
import 'package:flutter/material.dart';

class EmergencyReelItem extends StatelessWidget {
  const EmergencyReelItem({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
